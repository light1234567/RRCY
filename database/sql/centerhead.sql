-- Table structure for table `center_head`
DROP TABLE IF EXISTS `center_head`;

CREATE TABLE `center_head` (
  `name` VARCHAR(50) NOT NULL,
  PRIMARY KEY (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;

-- Insert data into `center_head`
INSERT INTO `center_head` (`name`) VALUES 
('ANGELIC B. PAÑA, RSW, MSSW');
